// Placeholder for local dev / `vite build`. In the Docker image this file is
// regenerated at container startup from FEEDBACK_SHARED_SECRET (see
// docker/30-generate-feedback-config.sh), so the real secret is never baked
// into the image.
window.__FEEDBACK_SECRET__ = "";
